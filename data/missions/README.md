# data/missions/

MissionData resources and the killzones/, tac_ops/, crit_ops/, kill_ops/
pools they draw from (see core/resources/mission_data.gd and
core/resources/killzone_data.gd). Populated in Phase 13 (Mission
generation), which reproduces the tabletop mission-generation procedure
rather than generating an arbitrary random battlefield.
